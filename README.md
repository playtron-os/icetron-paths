# icetron-paths

Locate an application's data and its bundled native libraries relative to the running
executable, so the same binary works on an FHS distribution and on one where nothing
lives under `/usr`.

Dependency-free and `std`-only.

## License

MIT
